import { OwnersHttpService } from './owners-http-service';

describe('OwnersHttpService', () => {
  it('should create an instance', () => {
    expect(new OwnersHttpService()).toBeTruthy();
  });
});
